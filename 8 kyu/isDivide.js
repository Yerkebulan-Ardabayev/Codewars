// Ваша задача состоит в том, чтобы создать функцию isDivideBy(или), проверяющую, делится is_divide_by ли целое число как на целые числа, так и на.number a b

function isDivideBy(number, a, b) {
  return number % a === 0 && number % b === 0;
}